apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: job-processor
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None  # Headless service — DNS directo al pod (postgres-0.postgres)
