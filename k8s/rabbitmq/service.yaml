apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: job-processor
spec:
  selector:
    app: rabbitmq
  ports:
    - name: amqp
      port: 5672
      targetPort: 5672
    - name: management
      port: 15672
      targetPort: 15672
  clusterIP: None  # Headless — DNS directo al pod para conexiones AMQP estables
